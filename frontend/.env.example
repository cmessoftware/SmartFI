# Frontend Environment Variables
# Leave unset in local dev — Vite proxies /api to 127.0.0.1:8000.
# Use 127.0.0.1, not localhost (Windows + Docker/WSL can hang on localhost:8000).
# VITE_API_URL=http://127.0.0.1:8000
